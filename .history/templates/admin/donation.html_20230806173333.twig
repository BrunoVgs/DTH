{% extends 'admin/base.html.twig' %}

{% dump(appointments)%}

{% block body %}
    <h1>Past Appointments</h1>
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Time</th>
                <th>Validation</th>
            </tr>
        </thead>
        <tbody>
            {% for appointment in appointments %}
                <tr>
                    <td>{{ appointment.dateTime|date('Y-m-d') }}</td>
                    <td>{{ appointment.dateTime|date('H:i') }}</td>
                    <td>
                        {# Add the validation form and buttons here #}
                        {# e.g., a form with a button to mark the appointment as validated #}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}